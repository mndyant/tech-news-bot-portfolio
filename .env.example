# Reference only: .env is not loaded automatically.
# Set these process environment variables only for live delivery.
# main.py --demo does not need any key or network access.
GEMINI_API_KEY=
DISCORD_WEBHOOK_DIGEST=
# Fallback when DIGEST is empty:
DISCORD_WEBHOOK_TECH=
